[tool.poetry]
name = "ips-mace"
version = "0.1.0"
description = "MACE IPSuite Plugin"
authors = ["zincwarecode <zincwarecode@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.10,<3.13"
mace-torch = "^0.3.6"
torch-dftd = "^0.4.0"
zntrack = "0.8.0a3"

[tool.poetry.group.dev.dependencies]
pytest = "^8.3.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
